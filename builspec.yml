version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - npm cache clean --force
      - npm i
  pre_build:
    commands:
      #- yarn build
  build:
    commands:
      - npx cypress run
