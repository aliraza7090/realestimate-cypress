version: 0.2

phases:
    commands:
      - npm cache clean --force
      - npm install -g n
      - n 14
      - npm i
  pre_build:
    commands:
      #- yarn build
  build:
    commands:
      - npx cypress run
